import {LoadingElem} from './LoadingElement.styled';
import React from 'react';

const LoadingElement: React.FC = (): JSX.Element => {
    return (
        <LoadingElem>
            loading...
        </LoadingElem>
    );
};

export default LoadingElement;
